export const APP_VERSION = '0.1.0';

export const GA_API = process.env.REACT_APP_GA_API;
export const ENCRYPTION_SECRET = process.env.REACT_APP_ENCRYPTION_SECRET;

export const API_HOST_URL = 'https://api.spacexdata.com/v2';

export const PAST_LAUNCHES_URL = `${API_HOST_URL}/launches`;

export const UPCOMING_LAUNCHES_URL = `${API_HOST_URL}/launches/upcoming`;

// export const NEXT_LAUNCH_URL = `${API_HOST_URL}/launches/next`;

// export const ALL_LAUNCHES_URL = `${API_HOST_URL}/launches/all`;
